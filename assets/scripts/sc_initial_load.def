
reactor_set(
    10,
    10,
    new_tile(
        new_cmp(
            NK_COMPONENT_PLATING,
            NK_PLATING_CONCRETE
        )
    )
)
reactor_set(
    11,
    10,
    new_tile(
        new_cmp(
            NK_COMPONENT_VENTS,
            NK_VENT_BASIC
        )
    )
)
reactor_set(
    12,
    10,
    new_tile(
        new_cmp(
            NK_COMPONENT_FUEL_CELL,
            NK_FUEL_CELL_URANIUM
        )
    )
)
reactor_set(
    12,
    11,
    new_tile(
        new_cmp(
            NK_COMPONENT_FUEL_CELL,
            NK_FUEL_CELL_THORIUM
        )
    )
)
reactor_set(
    13,
    10,
    new_tile(
        new_cmp(
            NK_COMPONENT_VENTS,
            NK_VENT_BASIC
        )
    )
)
if(!is_active(4,3)) {
    print("%s", "wtf")
}
__C__ {
    for(NkUInt16 i = 0; i < NK_COMPONENT_CATEGORIES_COUNT; i++)
    {
        NkComponentCategoryTable cat = gNkComponentCategories[(NkInt16) i];
        NK_PRINTLN("%d --  %s", i, gNkComponentCategoryNamesTable[i]);
        for(NkUInt16 j = 0; j < cat.count; j++)
        {
            NK_PRINTLN("\t%d = [%s]", cat.array[j].id.id, cat.array[j].name);
            NK_PRINTLN("\t\tHeat Out: %5.4f", cat.array[j].heatOutput);
            NK_PRINTLN("\t\tPower Out: %5.4f", cat.array[j].powerOutput);
        }
    }
}
print("What is at 4,3: %s", component_get(reactor_get(4,3).id)->name)